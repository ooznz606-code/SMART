{
  "$schema": "https://schema.railway.app/railway-schema.json",
  "deploy": {
    "startCommand": "python -m uvicorn license_server.server:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
